# MCP Resources

Drop sensor datasheets (PDF) into `datasheets/` using the filenames referenced
by the cards' `datasheet_uri` fields. The sensor cards themselves are served
automatically as `card://<sensor_type_id>` resources; nothing to do here for
those.
